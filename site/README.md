# TouchGrass landing page

This is a dependency-free static site. Deploy the `site` directory to Vercel:

```sh
npx vercel --cwd site
```

Upload the Android artifact to the repository’s GitHub Release as `TouchGrass.apk`; the landing page download buttons already target that release asset.
